package CodeWar.ResponsibleDrinking;

import javax.swing.*;
import java.util.regex.Matcher;
import java.util.regex.Pattern;

public class Drinkin {
    public static void main (String [] args){

        /*String aux = JOptionPane.showInputDialog("Insira um nome com um número: ");
        Pattern pattern = Pattern.compile("[0-9]");
        Matcher matcher = pattern.matcher(aux);
        boolean aux1 = matcher.find();
        int aux2 = Integer.valueOf(matcher.group());
        JOptionPane.showMessageDialog(null, matcher.group());*/

    }
}
